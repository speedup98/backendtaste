"use strict";

const srcdir = '/src';
const dirlist = {
    dirmodels : appRoot + srcdir + '/models',
    dirdatabases : appRoot + srcdir + '/databases',
};

module.exports = {
    dirlist : dirlist
};